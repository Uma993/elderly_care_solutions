# Firebase Admin (path to service account JSON)
GOOGLE_APPLICATION_CREDENTIALS=

# JWT for auth
JWT_SECRET=

# WebAuthn (optional; default inferred from origin)
# WEBAUTHN_RP_ID=localhost

# Web Push: run `npx web-push generate-vapid-keys`
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=

# AI: Google Gemini (get free key at https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=

# Wellbeing check: daily Web Push time for elders (24h)
WELLBEING_CHECK_HOUR=9
WELLBEING_CHECK_MINUTE=0

# Inactive tracker: alert family if elder has no activity for this many hours
INACTIVE_THRESHOLD_HOURS=24

# Fitbit Web API (register app at https://dev.fitbit.com/apps)
FITBIT_CLIENT_ID=
FITBIT_CLIENT_SECRET=
FITBIT_REDIRECT_URI=http://localhost:4000/api/fitbit/callback
FRONTEND_BASE_URL=http://localhost:5173

# Emotion detection (Python service - run: cd emotion-service && uvicorn main:app --port 5001)
EMOTION_SERVICE_URL=http://localhost:5001
