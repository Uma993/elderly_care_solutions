# Firebase Admin (path to service account JSON)
GOOGLE_APPLICATION_CREDENTIALS=

# JWT for auth
JWT_SECRET=

# WebAuthn (optional; default inferred from origin)
# WEBAUTHN_RP_ID=localhost

# Web Push: run `npx web-push generate-vapid-keys`
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=

# AI: Google Gemini (get free key at https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=
